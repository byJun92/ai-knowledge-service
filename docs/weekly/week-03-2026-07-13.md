# AI 지식·에이전트 엔지니어링 프로젝트 — Week 03

- 작성일: 2026-07-13
- 현재 주차: 3 / 24
- 누적 진행률: 12.5%
- 환경: macOS, Python 3.13, uv
- 저장소: `byJun92/ai-knowledge-service`
- 권장 경로: `docs/weekly/week-03-2026-07-13.md`

---

## 1. 이번 주 목표

Week 03에서는 도메인 코드가 저장 방식이나 FastAPI에 직접 의존하지 않도록 경계를 만든다.

핵심 결과물:

- `DocumentRepository` Protocol
- `InMemoryDocumentRepository`
- `DocumentService`
- Repository와 Service 단위 테스트
- 비상속 fake 구현
- `pre-commit`
- `AGENTS.md`
- Task Specification
- ADR-0003

구조:

```text
FastAPI(Week 04)
      ↓
DocumentService
      ↓
DocumentRepository Protocol
      ↑
InMemoryDocumentRepository
PostgresDocumentRepository(향후)
FakeDocumentRepository(테스트)
```

핵심 원칙:

```text
도메인은 FastAPI와 DB를 모른다.
Service는 구체 저장소를 모른다.
Repository는 HTTP 응답과 비즈니스 정책을 모른다.
```

---

## 2. Week 02 완료 판정

확인된 기반:

- `pydantic-settings`
- `.env.example`
- `SecretStr`
- `ApplicationError`
- `structlog`
- `mypy` strict
- pytest fixture
- 설정·예외·로그 테스트
- ADR-0002
- Week 02 작업의 `main` 병합

README의 일부 문서 정리는 Week 03 진행을 막는 필수 조건이 아니다.

---

## 3. Protocol과 ABC

### Protocol

`Protocol`은 필요한 메서드와 속성의 형태를 타입 계약으로 정의한다.

구현 클래스가 Protocol을 직접 상속하지 않아도 같은 메서드 계약을 만족하면 사용할 수 있다.

```python
from typing import Protocol


class Writer(Protocol):
    def write(self, text: str) -> None:
        ...


class ConsoleWriter:
    def write(self, text: str) -> None:
        print(text)
```

`ConsoleWriter`는 `Writer`를 상속하지 않았지만 `mypy` 기준으로 계약을 만족한다.

Java 비교:

```java
public interface Writer {
    void write(String text);
}
```

차이점:

| Java interface | Python Protocol |
|---|---|
| `implements` 필요 | 명시적 상속 불필요 |
| 컴파일러 검사 | mypy 등 별도 검사 |
| 명목적 타입 | 구조적 타입 |
| DI 계약 | 생성자 타입 계약 |

Protocol은 런타임에서 타입을 자동 강제하지 않으므로 다음 명령이 중요하다.

```bash
uv run mypy src tests
```

### ABC

`ABC`는 추상 기반 클래스다.

```python
from abc import ABC, abstractmethod


class AbstractWriter(ABC):
    @abstractmethod
    def write(self, text: str) -> None:
        ...
```

ABC가 적합한 상황:

- 공통 구현 공유
- Template Method
- 하위 클래스의 명시적 상속
- 런타임 추상 메서드 강제

이번 Repository는 작은 계약과 fake 교체가 중요하므로 실제 구현에는 Protocol을 사용한다.

---

## 4. Repository와 Service 경계

### Repository가 담당하는 것

- 저장
- ID 조회
- 목록 조회
- 향후 삭제와 검색

Repository가 담당하지 않는 것:

- HTTP status
- 입력 문구 검증
- 중복 처리 정책
- LLM 호출
- 사용자 메시지

### Service가 담당하는 것

- 입력 정규화
- 도메인 객체 생성
- 중복 검사
- Repository 호출 순서
- 애플리케이션 예외
- 구조화 로그

Service가 담당하지 않는 것:

- SQL
- FastAPI Request
- DB Session 생성
- 환경변수 직접 읽기

---

## 5. 브랜치와 디렉터리 생성

```bash
cd ~/workspace/ai-knowledge-service

git status
git switch main
git pull origin main
git switch -c feature/week-03-boundaries

uv add --dev pre-commit

mkdir -p src/ai_knowledge_service/repositories
mkdir -p src/ai_knowledge_service/services
mkdir -p tests/repositories
mkdir -p tests/services
mkdir -p docs/tasks

touch src/ai_knowledge_service/repositories/__init__.py
touch src/ai_knowledge_service/repositories/document_repository.py
touch src/ai_knowledge_service/repositories/in_memory_document_repository.py
touch src/ai_knowledge_service/services/__init__.py
touch src/ai_knowledge_service/services/document_service.py
touch tests/repositories/test_in_memory_document_repository.py
touch tests/services/test_document_service.py
touch .pre-commit-config.yaml
touch AGENTS.md
touch docs/tasks/week-03-repository-service.md
touch docs/adr/0003-repository-service-boundary.md
```

---

## 6. Repository Protocol

파일:

```text
src/ai_knowledge_service/repositories/document_repository.py
```

```python
from typing import Protocol

from ai_knowledge_service.domain.document import Document
from ai_knowledge_service.types import DocumentId


class DocumentRepository(Protocol):
    def save(self, document: Document) -> None:
        ...

    def find_by_id(self, document_id: DocumentId) -> Document | None:
        ...

    def list_all(self) -> tuple[Document, ...]:
        ...
```

설계 판단:

- 저장 기술을 외부에 노출하지 않는다.
- Service가 필요한 최소 메서드만 둔다.
- 구현체의 상속을 요구하지 않는다.

---

## 7. In-memory Repository

파일:

```text
src/ai_knowledge_service/repositories/in_memory_document_repository.py
```

```python
from ai_knowledge_service.domain.document import Document
from ai_knowledge_service.types import DocumentId


class InMemoryDocumentRepository:
    def __init__(self) -> None:
        self._documents: dict[DocumentId, Document] = {}

    def save(self, document: Document) -> None:
        self._documents[document.document_id] = document

    def find_by_id(self, document_id: DocumentId) -> Document | None:
        return self._documents.get(document_id)

    def list_all(self) -> tuple[Document, ...]:
        return tuple(self._documents.values())
```

이 클래스는 `DocumentRepository`를 상속하지 않지만 같은 메서드 형태를 만족한다.

---

## 8. 예외 추가

파일:

```text
src/ai_knowledge_service/exceptions.py
```

추가:

```python
class DocumentAlreadyExistsError(ApplicationError):
    code = "document_already_exists"
```

예외 구조:

```text
ApplicationError
├── InvalidDocumentError
├── DocumentAlreadyExistsError
├── DocumentNotFoundError
└── ExternalServiceError
```

---

## 9. DocumentService

파일:

```text
src/ai_knowledge_service/services/document_service.py
```

```python
from ai_knowledge_service.domain.document import Document, DocumentType
from ai_knowledge_service.exceptions import (
    DocumentAlreadyExistsError,
    DocumentNotFoundError,
    InvalidDocumentError,
)
from ai_knowledge_service.observability.logging import get_logger
from ai_knowledge_service.repositories.document_repository import DocumentRepository
from ai_knowledge_service.types import DocumentId


class DocumentService:
    def __init__(self, repository: DocumentRepository) -> None:
        self._repository = repository
        self._logger = get_logger(__name__)

    def register(
        self,
        *,
        document_id: str,
        title: str,
        source_url: str,
        document_type: DocumentType,
    ) -> Document:
        try:
            document = Document.create(
                document_id=document_id,
                title=title,
                source_url=source_url,
                document_type=document_type,
            )
        except ValueError as error:
            raise InvalidDocumentError(
                str(error),
                details={"document_id": document_id},
            ) from error

        existing = self._repository.find_by_id(document.document_id)

        if existing is not None:
            raise DocumentAlreadyExistsError(
                "Document already exists",
                details={"document_id": str(document.document_id)},
            )

        self._repository.save(document)

        self._logger.info(
            "document_registered",
            document_id=str(document.document_id),
            document_type=document.document_type.value,
        )

        return document

    def get(self, document_id: str) -> Document:
        normalized_id = document_id.strip()

        if not normalized_id:
            raise InvalidDocumentError(
                "document_id must not be blank",
                details={"document_id": document_id},
            )

        document = self._repository.find_by_id(DocumentId(normalized_id))

        if document is None:
            raise DocumentNotFoundError(
                "Document was not found",
                details={"document_id": normalized_id},
            )

        return document

    def list_documents(self) -> tuple[Document, ...]:
        return self._repository.list_all()
```

문서 등록 흐름:

```text
입력
 ↓
Document.create 검증
 ↓
Repository 중복 조회
 ↓
Repository 저장
 ↓
document_registered 로그
 ↓
Document 반환
```

---

## 10. Repository 테스트

파일:

```text
tests/repositories/test_in_memory_document_repository.py
```

```python
from ai_knowledge_service.domain.document import Document, DocumentType
from ai_knowledge_service.repositories.in_memory_document_repository import (
    InMemoryDocumentRepository,
)
from ai_knowledge_service.types import DocumentId


def test_save_and_find_document() -> None:
    repository = InMemoryDocumentRepository()
    document = Document.create(
        document_id="doc-001",
        title="Repository Pattern",
        source_url="https://example.com/repository-pattern",
        document_type=DocumentType.TECH_ARTICLE,
    )

    repository.save(document)

    assert repository.find_by_id(document.document_id) is document


def test_list_all_documents() -> None:
    repository = InMemoryDocumentRepository()
    first = Document.create(
        document_id="doc-001",
        title="First",
        source_url="https://example.com/first",
        document_type=DocumentType.TECH_ARTICLE,
    )
    second = Document.create(
        document_id="doc-002",
        title="Second",
        source_url="https://example.com/second",
        document_type=DocumentType.DOCUMENTATION,
    )

    repository.save(first)
    repository.save(second)

    assert repository.list_all() == (first, second)


def test_find_returns_none_when_document_does_not_exist() -> None:
    repository = InMemoryDocumentRepository()

    assert repository.find_by_id(DocumentId("missing")) is None
```

---

## 11. pytest fixture 추가

파일:

```text
tests/conftest.py
```

기존 fixture는 유지하고 다음을 추가한다.

```python
from ai_knowledge_service.repositories.in_memory_document_repository import (
    InMemoryDocumentRepository,
)
from ai_knowledge_service.services.document_service import DocumentService


@pytest.fixture
def document_repository() -> InMemoryDocumentRepository:
    return InMemoryDocumentRepository()


@pytest.fixture
def document_service(
    document_repository: InMemoryDocumentRepository,
) -> DocumentService:
    return DocumentService(document_repository)
```

---

## 12. Service 테스트

파일:

```text
tests/services/test_document_service.py
```

```python
import pytest

from ai_knowledge_service.domain.document import Document, DocumentType
from ai_knowledge_service.exceptions import (
    DocumentAlreadyExistsError,
    DocumentNotFoundError,
    InvalidDocumentError,
)
from ai_knowledge_service.services.document_service import DocumentService
from ai_knowledge_service.types import DocumentId


def test_register_and_get_document(
    document_service: DocumentService,
) -> None:
    created = document_service.register(
        document_id="doc-001",
        title="Dependency Inversion",
        source_url="https://example.com/dependency-inversion",
        document_type=DocumentType.TECH_ARTICLE,
    )

    assert document_service.get("doc-001") is created


def test_register_rejects_duplicate_document(
    document_service: DocumentService,
) -> None:
    document_service.register(
        document_id="doc-001",
        title="First",
        source_url="https://example.com/first",
        document_type=DocumentType.TECH_ARTICLE,
    )

    with pytest.raises(DocumentAlreadyExistsError):
        document_service.register(
            document_id="doc-001",
            title="Duplicate",
            source_url="https://example.com/duplicate",
            document_type=DocumentType.TECH_ARTICLE,
        )


def test_register_converts_domain_error(
    document_service: DocumentService,
) -> None:
    with pytest.raises(InvalidDocumentError):
        document_service.register(
            document_id="",
            title="Invalid",
            source_url="https://example.com/invalid",
            document_type=DocumentType.TECH_ARTICLE,
        )


def test_get_raises_when_document_does_not_exist(
    document_service: DocumentService,
) -> None:
    with pytest.raises(DocumentNotFoundError):
        document_service.get("missing")


def test_list_documents(
    document_service: DocumentService,
) -> None:
    first = document_service.register(
        document_id="doc-001",
        title="First",
        source_url="https://example.com/first",
        document_type=DocumentType.TECH_ARTICLE,
    )
    second = document_service.register(
        document_id="doc-002",
        title="Second",
        source_url="https://example.com/second",
        document_type=DocumentType.DOCUMENTATION,
    )

    assert document_service.list_documents() == (first, second)


class RecordingDocumentRepository:
    def __init__(self) -> None:
        self.saved: list[Document] = []

    def save(self, document: Document) -> None:
        self.saved.append(document)

    def find_by_id(self, document_id: DocumentId) -> Document | None:
        for document in self.saved:
            if document.document_id == document_id:
                return document
        return None

    def list_all(self) -> tuple[Document, ...]:
        return tuple(self.saved)


def test_service_accepts_non_inheriting_fake() -> None:
    repository = RecordingDocumentRepository()
    service = DocumentService(repository)

    created = service.register(
        document_id="doc-001",
        title="Structural Typing",
        source_url="https://example.com/structural-typing",
        document_type=DocumentType.TECH_ARTICLE,
    )

    assert repository.saved == [created]
```

마지막 테스트는 fake가 Protocol을 상속하지 않아도 Service에 주입되는 것을 확인한다.

---

## 13. pre-commit 설정

파일:

```text
.pre-commit-config.yaml
```

```yaml
minimum_pre_commit_version: "4.0.0"

default_install_hook_types:
  - pre-commit
  - pre-push

repos:
  - repo: local
    hooks:
      - id: ruff-check
        name: Ruff lint
        entry: uv run ruff check .
        language: system
        pass_filenames: false
        stages: [pre-commit]

      - id: ruff-format-check
        name: Ruff format check
        entry: uv run ruff format --check .
        language: system
        pass_filenames: false
        stages: [pre-commit]

      - id: mypy
        name: mypy strict
        entry: uv run mypy src tests
        language: system
        pass_filenames: false
        stages: [pre-commit]

      - id: pytest
        name: pytest
        entry: uv run pytest --cov=ai_knowledge_service
        language: system
        pass_filenames: false
        stages: [pre-push]
```

설치:

```bash
uv run pre-commit install   --hook-type pre-commit   --hook-type pre-push
```

수동 검증:

```bash
uv run pre-commit validate-config
uv run pre-commit run --all-files
uv run pre-commit run --hook-stage pre-push --all-files
```

---

## 14. AGENTS.md

저장소 루트:

```text
AGENTS.md
```

권장 내용:

```markdown
# AGENTS.md

## Project purpose

Document ingestion, retrieval, RAG, and multi-agent AI knowledge service.

## Runtime

- Python 3.13
- Use uv only.
- Do not use global pip.
- Do not edit uv.lock manually.

## Architecture

- Domain must not import FastAPI, DB clients, or LLM SDKs.
- Services depend on Protocols, not concrete repositories.
- Repositories must not contain HTTP response logic.
- API layers must not access storage directly.
- Deterministic validation belongs in Python code, not prompts.

## Required validation

```bash
uv sync
uv run ruff check .
uv run ruff format --check .
uv run mypy src tests
uv run pytest --cov=ai_knowledge_service
uv run pre-commit run --all-files
```

## Security

- Never commit .env, API keys, tokens, passwords, or private data.
- Never log secrets.
- Do not weaken secret-related .gitignore rules.

## Git safety

Do not commit, push, merge, force-push, delete branches, create tags,
or run destructive Git commands unless explicitly requested.

## Definition of Done

- requested behavior implemented
- architecture boundaries preserved
- success and failure tests added
- Ruff passes
- formatting passes
- mypy passes
- pytest and coverage pass
- documentation updated
- no secret or unrelated change included
```

루트 `AGENTS.md`는 저장소 전체 규칙으로 사용한다. 특정 하위 경로에 별도 규칙이 필요할 때만 더 가까운 위치에 추가한다.

---

## 15. Task Specification

파일:

```text
docs/tasks/week-03-repository-service.md
```

```markdown
# Task Specification: Week 03 Repository and Service Boundary

## Goal

Create a storage-independent DocumentService using a DocumentRepository
Protocol and an in-memory adapter.

## In scope

- Repository Protocol
- memory adapter
- Service
- tests and fake
- pre-commit
- AGENTS.md
- ADR

## Out of scope

- FastAPI
- PostgreSQL
- async
- LLM
- embeddings

## Acceptance criteria

- valid registration succeeds
- duplicate registration fails
- invalid input fails
- missing document fails
- list returns documents
- non-inheriting fake satisfies the contract
- Ruff, mypy, pytest, coverage and pre-commit pass
```

---

## 16. ADR-0003

파일:

```text
docs/adr/0003-repository-service-boundary.md
```

```markdown
# ADR-0003: Protocol 기반 Repository와 Service 경계

## 상태

Accepted

## 배경

FastAPI와 PostgreSQL을 도메인 코드에 직접 연결하면 저장 기술 교체와 단위
테스트가 어려워진다.

## 결정

- DocumentRepository를 Protocol로 정의한다.
- DocumentService는 Protocol에만 의존한다.
- 첫 Adapter는 InMemoryDocumentRepository다.
- 중복 정책과 사용 사례 순서는 Service가 담당한다.
- 저장 세부사항은 Repository가 담당한다.
- FastAPI와 PostgreSQL은 이후 Adapter로 추가한다.
- 로컬 검증은 pre-commit으로 자동화한다.
- Coding Agent 규칙은 AGENTS.md에 기록한다.

## 결과

장점:

- 저장 기술 교체 가능
- fake 테스트 간단
- FastAPI와 도메인 분리
- Agent Tool 계약으로 확장 가능

비용:

- 파일과 경계 증가
- Protocol 변경 시 구현체 수정 필요
- mypy 실행 필수
```

---

## 17. 최종 파일 구조

```text
ai-knowledge-service/
├── .pre-commit-config.yaml
├── AGENTS.md
├── pyproject.toml
├── uv.lock
├── docs/
│   ├── adr/
│   │   └── 0003-repository-service-boundary.md
│   ├── tasks/
│   │   └── week-03-repository-service.md
│   └── weekly/
│       └── week-03-2026-07-13.md
├── src/ai_knowledge_service/
│   ├── repositories/
│   │   ├── document_repository.py
│   │   └── in_memory_document_repository.py
│   └── services/
│       └── document_service.py
└── tests/
    ├── repositories/
    │   └── test_in_memory_document_repository.py
    └── services/
        └── test_document_service.py
```

---

## 18. 전체 검증

```bash
uv run ruff check . --fix
uv run ruff format .

uv sync   && uv run ruff check .   && uv run ruff format --check .   && uv run mypy src tests   && uv run pytest --cov=ai_knowledge_service   && uv run pre-commit run --all-files
```

pre-push 검사:

```bash
uv run pre-commit run --hook-stage pre-push --all-files
```

---

## 19. 자주 발생하는 오류

### mypy가 구현체를 Protocol로 인정하지 않음

메서드명, 인자 타입, 반환 타입을 확인한다.

```python
def find_by_id(self, document_id: DocumentId) -> Document | None:
    ...
```

### Service가 구체 Repository를 직접 생성함

잘못된 구조:

```python
class DocumentService:
    def __init__(self) -> None:
        self._repository = InMemoryDocumentRepository()
```

권장 구조:

```python
class DocumentService:
    def __init__(self, repository: DocumentRepository) -> None:
        self._repository = repository
```

### coverage 하락

다음 분기를 모두 테스트한다.

- 정상 등록
- 중복
- 잘못된 입력
- 정상 조회
- 없는 문서
- 목록
- fake 구현

### pre-push hook가 동작하지 않음

```bash
uv run pre-commit install   --hook-type pre-commit   --hook-type pre-push
```

---

## 20. 권장 커밋

```bash
git add pyproject.toml uv.lock .pre-commit-config.yaml
git commit -m "chore: add pre-commit quality hooks"

git add src/ai_knowledge_service/repositories
git commit -m "feat: add document repository protocol and memory adapter"

git add src/ai_knowledge_service/services src/ai_knowledge_service/exceptions.py
git commit -m "feat: add document service boundary"

git add tests
git commit -m "test: verify repository and service behavior"

git add AGENTS.md docs
git commit -m "docs: add coding agent rules and week 03 guide"
```

완료 후:

```bash
git push -u origin feature/week-03-boundaries
```

모든 수정이 `main`에 반영된 다음 태그를 만든다.

```bash
git tag -a week-03-complete -m "Complete week 03 boundaries"
git push origin week-03-complete
```

---

## 21. Harness와 Loop 연결

이번 주 구성요소는 이후 다음 항목으로 확장된다.

| Week 03 | 향후 Harness |
|---|---|
| Repository Protocol | StateStore, CheckpointStore |
| Service | deterministic Worker |
| fake Repository | fake Tool, fake LLM |
| pre-commit | Verifier |
| AGENTS.md | Coding Agent 작업 정책 |
| Task Specification | Goal, Scope, Success Condition |
| ApplicationError | retry, fallback, escalation |
| 구조화 로그 | ExecutionRecorder, Trace |

결정적인 검증·검색·저장은 Python 코드와 Tool이 수행한다.

LLM은 분류·해석·요약·종합에 집중한다.

---

## 22. 최신 관련 연구

### AI Harness Engineering: A Runtime Substrate for Foundation-Model Software Agents

- 저자: Hailin Zhong, Shengxin Zhu
- 공개일: 2026-05-13
- 출처: arXiv

핵심 관점:

```text
Model + Harness + Environment
```

제안된 Harness 책임에는 task specification, context selection, tool access,
project memory, task state, observability, failure attribution, verification,
permissions, entropy auditing, intervention recording이 포함된다.

이번 주 연결:

- Task Specification
- AGENTS.md
- pre-commit 검증
- 예외 계층
- ADR
- 구조화 로그

해석:

좋은 모델만으로 안정적인 코딩 자동화가 완성되는 것이 아니라, 작업 범위와
검증 증거를 제공하는 Harness가 함께 필요하다.

---

## 23. 다음 주

Week 04:

- FastAPI
- Router
- Pydantic 요청·응답 DTO
- Dependency Injection
- Service 연결
- 예외 응답
- API 테스트

요청 흐름:

```text
HTTP Request
    ↓
FastAPI Router
    ↓
Request DTO
    ↓
DocumentService
    ↓
DocumentRepository
```

---

## 24. 완료 체크리스트

### 코드

- [ ] DocumentRepository Protocol
- [ ] InMemoryDocumentRepository
- [ ] DocumentService
- [ ] DocumentAlreadyExistsError
- [ ] Service가 구체 저장소를 import하지 않음

### 테스트

- [ ] Repository 저장·조회
- [ ] 목록 조회
- [ ] 정상 등록·조회
- [ ] 중복
- [ ] 잘못된 입력
- [ ] 없는 문서
- [ ] 비상속 fake
- [ ] coverage 80% 이상

### 자동화·문서

- [ ] pre-commit 설치
- [ ] pre-commit/pre-push hook
- [ ] AGENTS.md
- [ ] Task Specification
- [ ] ADR-0003
- [ ] Week 03 문서
- [ ] 전체 검사 통과
- [ ] main 반영
- [ ] week-03-complete 태그

---

## 25. 참고 자료

- Python 공식 문서: `typing.Protocol`
- Python 공식 문서: `abc.ABC`, `abstractmethod`
- pre-commit 공식 문서
- OpenAI Codex 문서: `AGENTS.md`
- *AI Harness Engineering: A Runtime Substrate for Foundation-Model Software Agents*
